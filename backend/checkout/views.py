from .models import Bill, SellerOrder, Order, PaymentMethod
from django.views.generic import CreateView, UpdateView, DetailView, ListView
from django.shortcuts import render

# Create your views here.
